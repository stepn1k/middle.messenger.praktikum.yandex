export default `   
   <div class="form-field">
        <label for="{{ id }}" class="form-field__label">{{ labelText }}</label>
        <input class="form-field__input" required id="{{ id }}" type="{{ type }}" name="{{ id }}">
   </div>
`